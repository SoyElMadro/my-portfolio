{
  Astro.props.isTextArea ? (
    <slot />
  ) : (
    <div class="flex flex-col">
      <label class="mb-1 max-sm:text-sm" for={Astro.props.id}>
        {Astro.props.label}
      </label>
      <input
        class="p-2 text-md rounded-md max-sm:text-sm"
        id={Astro.props.id}
        type="text"
        placeholder={Astro.props.placeholder}
      />
    </div>
  )
}
